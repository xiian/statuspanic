define(['models/module', 'views/module'], function(){
    var View = Backbone.View.extend({
        initialize: function(options) {
        },

        render: function() {
        }
    });
    return View;
});
